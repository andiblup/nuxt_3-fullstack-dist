import { describe, it, expect } from 'vitest'

describe('Sanity', () => {
  it('should add numbers', () => {
    expect(2 + 2).toBe(4)
  })
})
// This is a simple test to ensure that the testing setup is working correctly.